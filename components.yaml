components:
- name: ocm.software/ocmcli-image
  version: ${VERSION}
  provider:
    name: open-component-model
  resources:
  - name: ocm-image
    type: ociImage
    version: ${VERSION}
    access:
      type: ociArtifact
      imageReference: ghcr.io/jensh007/ocm-cli:v${VERSION}
